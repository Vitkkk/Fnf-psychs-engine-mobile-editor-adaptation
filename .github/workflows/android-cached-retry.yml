name: Android cached retry (manual only)

on:
  workflow_dispatch:

concurrency:
  group: psych-mobile-android-build
  cancel-in-progress: true

jobs:
  notice:
    runs-on: ubuntu-22.04
    steps:
      - name: Retry moved to primary build workflow
        run: echo "Automatic heavy retries are disabled. Use the single Android APK build workflow instead."
